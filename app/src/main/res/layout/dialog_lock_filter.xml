<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/default_dialog_background"
    android:padding="20dp">

    <TextView
        android:id="@+id/dialog_lock_filter_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:text="@string/dialog_lock_filter_title"
        android:textColor="@color/gray_200"
        android:textSize="20sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/dialog_lock_filter_message"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/dialog_lock_filter_title"
        android:layout_marginTop="8dp"
        android:text="@string/dialog_lock_filter_message"
        android:textColor="@color/gray_200"
        android:textSize="16sp" />

    <ListView
        android:id="@+id/dialog_lock_filter_list_view"
        android:layout_width="match_parent"
        android:layout_height="250dp"
        android:layout_below="@id/dialog_lock_filter_message"
        android:layout_marginTop="8dp"
        android:background="@drawable/edit_text_background"
        android:divider="@color/edit_text_background_color"
        android:dividerHeight="1dp" />

    <ProgressBar
        android:id="@+id/dialog_lock_filter_progress_bar"
        style="?android:attr/progressBarStyle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:visibility="gone" />

</RelativeLayout>
